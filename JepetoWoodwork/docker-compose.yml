version: "3.4"

services:
  web:
    image: danieii/jepetowoodwork
    platform: linux/amd64
    build: .
    command: gunicorn --bind=0.0.0.0:8000 JepetoWoodwork.wsgi
    working_dir: /app
    ports:
      - "8000:8000"
    env_file:
      - ./.env
    volumes:
      - /home/ubuntu/app/staticfiles:/app/staticfiles
      - /home/ubuntu/app/media:/app/media
    depends_on:
      - db

  db:
    image: postgres
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: jepeto_db
      PGDATA: /var/lib/postgresql/data/pgdata
    env_file:
      - ./.env
    volumes:
      - ./postgresql:/var/lib/postgresql/data

  nginx:
    image: nginx
    ports:
      - "81:80"
      - "443:443"
    volumes:
      - /home/ubuntu/app/staticfiles:/app/staticfiles
      - /home/ubuntu/app/media:/app/media
      - ./nginx/conf.d:/etc/nginx/conf.d
      - ./certbot/www:/var/www/certbot/:ro
      - ./certbot/conf/:/etc/nginx/ssl/:ro
    links:
      - web
